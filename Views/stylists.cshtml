@{
    Layout="Shared/_Layout.cshtml";
}
@if(Model.Count == 0)
{
    <h5>There are no stylists listed</h5>

}
else
{
    <div class="">
        <h3>Stylist View</h3>
    </div>
    <div class="col s4">
    @foreach(var entry in Model)
    {
        <div class="stylist-holder">
        <a href="/stylist_clients/@entry.GetId()" id="@entry.GetName()"><h5>@entry.GetName()</h5></a>
        </div>
    }
    </div>
}
